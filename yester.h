#ifndef YESTER_H
#define YESTER_H

#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <string.h>

char *yesterweek(unsigned short weeklen);
void weekloop(unsigned short weeklen);

#endif
